--CAMBIOS--

1) 
	Relacion entre linea y parada pasa de 
	1 -> 1..N
 	a 
	0..N -> 1..N
	Porque de lo contrario una parada solo podría relacionarse con una sola línea.

2)

	PostgreSQL no implementa herencia con foreing keys y primary keys, y buscar la forma de crear una herencia 
	que funcione es demasiado problematico. La solucion simple para la línea es crear una única tabla que 
	engloble todos los atributos y tenga un atributo "tipo" que indique si es económica o superior.


